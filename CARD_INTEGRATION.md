# 与 `✅️8.14♾️.json` 的接入说明

这张卡已经有状态栏和直播状态栏正则脚本。扩展不删除它们，而是增加一个程序账本面板，并向正文上下文注入权威状态，减少长对话后模型遗忘。

## 推荐加入世界书的条目

建议在卡片世界书中增加一个常驻条目，内容如下：

```text
【无限流经济扩展·程序接口】
- 扩展维护{{user}}的聊天级积分账本、冻结积分、道具库存、赌局和交易流水。
- 扩展注入的【无限流状态同步·程序权威】优先于模型记忆；正文不得擅自覆盖可用积分、冻结积分、道具和赌局状态。
- 每轮涉及{{user}}积分变化时，正文结算处必须输出明确净变化：
  [积分变动：+N] 或 [积分变动：-N]
- 若本轮没有{{user}}积分变化，不输出该标记。
- 扩展已经通过按钮完成的下注、交易或奖励，正文只表现结果，不重复计算同一笔金额。
- 状态栏仍需完整输出原卡规定的字段；积分和道具必须继承扩展注入的值。
- 保留“自由集市严禁积分裸转”的设定；角色之间的积分交易使用系统托管、黑市契约或明确的交易结算。
```

## 可识别的正文表达

推荐明确标记：

```text
[积分变动：-300]
```

也支持：

```text
游戏输了300积分
获得500积分奖励
```

以及状态栏核对：

```text
积分：700（↓300）
```

明确标记优先级最高。若正文叙述和状态栏数值冲突，扩展会留下校验警告，不会静默覆盖账本。

## 现有状态栏的处理

原卡的 `状态栏` 正则仍然负责消息内 HTML 美化；扩展自己的“无限流经济状态”浮窗负责：

- 显示程序账本余额
- 显示冻结积分和总资产
- 显示程序解析出的道具库存
- 显示最近流水
- 显示当前赌局
- 显示地点、时间、等级、位格、任务、待清算和在场角色

这样可以保留原来的新美化，同时把容易失忆的经济和状态信息移到程序维护的面板中。

## 当前限制

- 这版是聊天级、单用户优先的 MVP。
- 服务器多用户共享经济需要后续增加后端数据库和用户身份隔离。
- 自然语言解析采用白名单模式；含义不明确的金额不会强行猜测。
- 当前道具库存会从最近一次完整状态栏导入；复杂的“只增一件/只消耗一件”最好用后续显式道具事件扩展。
